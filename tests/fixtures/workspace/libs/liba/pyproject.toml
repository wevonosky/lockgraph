[project]
name = "liba"
version = "0.1.0"
requires-python = ">=3.11"
readme = "README.md"
license-files = ["LICENSE"]
dependencies = ["core"]

[tool.uv.sources]
core = { workspace = true }

[build-system]
requires = ["uv_build>=0.10,<0.12"]
build-backend = "uv_build"
